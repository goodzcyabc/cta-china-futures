[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cta-china-futures"
version = "0.1.0"
description = "Contract-level CTA research and production system for Chinese commodity futures (trend + carry), built for deployability."
requires-python = ">=3.9"
dependencies = [
  "pandas>=2.2,<3", "numpy>=1.26,<3", "pyarrow>=14", "pydantic>=2", "pyyaml>=6", "typer>=0.12", "rich>=13",
  "scipy>=1.10", "statsmodels>=0.14", "matplotlib>=3.7",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.5", "mypy>=1.10", "pandas-stubs==2.2.2.240807", "types-PyYAML"]  # stubs 钉版本:CI 与本地 mypy 结果一致

[project.scripts]
cta = "cta.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 110
target-version = "py39"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "W", "SIM", "PL"]
ignore = ["PLR0913", "PLR2004", "PLR0912", "PLR0915", "E501", "PLR0917", "B008", "PLC0415"]  # 数值函数多参数 / typer 默认值惯用法 / CLI 延迟导入

[tool.mypy]
python_version = "3.10"  # mypy>=1.19 不再支持 3.9 目标;运行时仍支持 3.9(CI 矩阵)
strict = true
warn_unused_ignores = true
ignore_missing_imports = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
